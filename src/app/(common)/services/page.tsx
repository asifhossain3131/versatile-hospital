import getAllDoctor from '@/services/getAllDoctor.service';
import React from 'react';

const ServicesPage = async() => {
    return (
        <div>
            services
        </div>
    );
};

export default ServicesPage;